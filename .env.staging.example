# Copy to .env.staging (gitignored). CI should create the real file before build.
AUTH_DOMAIN=
AUTH_CLIENT_ID=
AUTH_AUDIENCE=
LAUNCHDARKLY_CLIENT_SIDE_ID=
LAUNCHDARKLY_MOBILE_KEY=
SUBSCRIPTION_BILLING_WEB_URL=
# Google Maps API key (enable Maps SDK for Android/iOS in Google Cloud Console).
# Release builds: run ./scripts/sync_ios_secrets.sh staging before archiving.
GOOGLE_MAPS_API_KEY=
